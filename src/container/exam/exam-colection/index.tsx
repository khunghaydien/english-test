import ExamColection from "@/component/exam/exam-colection/Colection";
import { connect } from "react-redux";
const mapStateToProps = (state: any) => ({
});
const mapDispatchToProps = (dispatch: any) => ({
});
export default connect(mapStateToProps, mapDispatchToProps)(ExamColection);
