import ExamSolution from "@/component/exam/exam-solution";
import { connect } from "react-redux";
const mapStateToProps = (state: any) => ({
});
const mapDispatchToProps = (dispatch: any) => ({
});
export default connect(mapStateToProps, mapDispatchToProps)(ExamSolution);
